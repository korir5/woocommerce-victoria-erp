# Installation

1. Copy the `victoria-erp-connector` folder into `wp-content/plugins/`.
2. Activate the plugin from the WordPress Plugins screen.
3. Configure `Base URL`, `Company Code`, and `API Key` under the Victoria ERP settings page.
4. Enable stock, price, product sync and/or promotion engine as needed.
5. Save settings and verify the connection.
